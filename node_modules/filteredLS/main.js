var fs = require('fs');

var path=process.argv[2];
var file_ext=process.argv[3];


var output = new Array();

fs.readdir(path,function(err, files){
    if(err){
        onerror();
        return;
    }
    for(var i =0; i<files.length;i++){
        //console.log(files[i]);
        if(files[i].split(".")[1]===file_ext){
          //files.unshift(files[i]);
          console.log(files[i]);
          //output.push(files[i]);
          //console.log(output);
        }
    }
    //console.log(output);
});
